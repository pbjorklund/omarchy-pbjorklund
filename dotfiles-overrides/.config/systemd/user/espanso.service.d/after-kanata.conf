[Unit]
# Ensure espanso starts after kanata to avoid input device conflicts
After=kanata.service
PartOf=kanata.service

[Service]
# Restart espanso when kanata restarts to re-establish input device access
Restart=on-failure
RestartSec=3s